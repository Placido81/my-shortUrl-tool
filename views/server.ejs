const express = require('express')
const app = express()

app.set('view engine', 'ejs')

app.get('/', (req, res) => {
	res.render('server', { myVariable: 'My name is Mark!' })
})

app.listen(process.env.PUBLIC_PORT, () => {
	console.log('Server started')
})